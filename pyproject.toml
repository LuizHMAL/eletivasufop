[project]
name = "eletivasufop"
version = "1.0.0"
description = "Eletivas UFOP - Backend FastAPI e Frontend React"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "pydantic>=2.0.0",
]

[tool.vercel]
entrypoint = "api.index:app"

